Nov 2017
==
Now using markdown and the dev files are in the main root under "htmldev". There has been no query from anyone to localize the help, so there's no guide. Contact us if you wish to try.

Old
==
To localize your help file, create a directory here using the two letter code,
for example France would be 'fr' (It should be the same as the code used in
the locale folder.)


:)
/d
